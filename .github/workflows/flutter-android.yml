name: Build Android APK and AAB
on:
  workflow_dispatch:
jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Create Android platform
        working-directory: flutter_app
        run: |
          flutter create . --platforms=android --project-name warqna_mobile --org com.warqna
          if [ -f test/widget_test.dart ] && grep -q "MyApp" test/widget_test.dart; then rm test/widget_test.dart; fi
      - run: flutter pub get
        working-directory: flutter_app
      - name: Configure Android application
        working-directory: flutter_app
        run: |
          python3 - <<'PYCODE'
          from pathlib import Path
          manifest = Path('android/app/src/main/AndroidManifest.xml')
          text = manifest.read_text()
          if 'android.permission.INTERNET' not in text:
              text = text.replace('<manifest xmlns:android="http://schemas.android.com/apk/res/android">', '<manifest xmlns:android="http://schemas.android.com/apk/res/android">\n    <uses-permission android:name="android.permission.INTERNET"/>')
          text = text.replace('android:label="warqna_mobile"', 'android:label="Warqna"')
          manifest.write_text(text)
          PYCODE
      - name: Analyze and build APK
        working-directory: flutter_app
        run: |
          API_URL="${{ vars.WARQNA_API_URL }}"
          EXTRA=""
          if [ -n "$API_URL" ]; then EXTRA="--dart-define=WARQNA_API_URL=$API_URL"; fi
          flutter analyze --no-fatal-infos --no-fatal-warnings
          flutter test
          flutter build apk --release $EXTRA
          flutter build appbundle --release $EXTRA
      - uses: actions/upload-artifact@v4
        with:
          name: warqna-v145-android
          path: |
            flutter_app/build/app/outputs/flutter-apk/app-release.apk
            flutter_app/build/app/outputs/bundle/release/app-release.aab
